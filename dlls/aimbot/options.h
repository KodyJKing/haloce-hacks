namespace Options {
    extern bool aimbot;
    extern bool esp;
    extern bool timeHack;
    extern bool freezeTime;
    extern bool showLines;
    extern bool showNumbers;
    extern bool showFrames;
    extern bool showHead;
    extern bool smoothTargeting;
    extern bool showAxes;
    extern bool showLookingAt;
    extern bool triggerbot;
    extern bool smartTargeting;
    extern bool showLabels;
    extern bool fullAuto;
}